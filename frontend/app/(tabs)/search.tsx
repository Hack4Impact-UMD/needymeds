import { View, Text } from 'react-native';

const SearchScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>This page shows the header</Text>
  </View>
);

export default SearchScreen;
